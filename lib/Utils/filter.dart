enum SortLibraryItem {
  title,
  publishYear,
  authorName,
  updated,
  added,
}

enum SortSeriesItem {
  numOfBooks,
  duration,
  addedAt,
}

enum SortAuthorItem {
  name,
  addedAt,
  numOfBooks,
}
